import React from 'react';

import {
  View
} from 'react-native';
import styles from "../main/styles";

const Search = () => (
  <View />
);

Search.navigationOptions = {
  title: "Buscar",
};

export default Search;
